# Reading record — README.md — 2026-08-13

Audited under text-audit (live-spec pack, v4.3.0), by-hand mode. The auditor is the session
preparing release 1.3.0. Primary surface: human prose; the README publishes outside the project, so
the artifact surface binds as well.

## Mechanical lints (grep fallbacks)

- **Term at first use** — hits: `sweep`, `probe` used without definitions in "What comes back";
  `surface` used in "Three modes" without one; `Jaccard overlap` named bare. Fixed: one-sentence
  definitions written at first use, each taken from `SKILL.md` (the source the text rests on).
- **Weak relational words** — hit: `depends` in the opening banner with an unfilled slot. Fixed:
  the sentence now states what runs without the pack.
- **Requirement shape / vocabulary / weak-word scripts** — spec-only lints; this text is a README,
  so each names its input set as empty. Recorded as the expected refusal, per the skill.
- **Style and register** — four sentences over the 25-word cap (two introduced during the 1.3.0
  rework). Fixed by splitting. No caps-for-emphasis hits beyond filenames and machine names. No
  neighbour-denying constructions. No grading adjectives.
- **One name per thing** — hits: `edition`/`release` for one thing (7 mentions unified on
  `release`); `evidence file`/`record` (unified on `record`); `package`/`skill` (unified on
  `skill`). `pass`/`review` name two different things in this skill's own vocabulary and stand.

## Cold readings

Did not run. The by-hand mode requires two fresh readers — one under the reader prompt, one under
the unprompted brief — and this session had no fresh reader at hand. Per the skill, the lint layer
ran whole, the auditor's own read repaired what it found, and the audit **stays open**. The cold
read is owed before this README is counted as read; a stranger's stops on the repaired text are the
findings this record is missing.

## Refutation

Auditor-run. Thrown out as false hits: the quoted sample finding (sample output, its register is
the review's own), the ask-phrase list, and the release-history bullets caught by the
sentence-length regex.
